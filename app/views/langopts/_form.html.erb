<%= form_for(@langopt) do |f| %>
  <% if @langopt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@langopt.errors.count, "error") %> prohibited this langopt from being saved:</h2>

      <ul>
      <% @langopt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
